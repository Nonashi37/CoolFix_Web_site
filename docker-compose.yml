services:
  web:
    build: .
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
      - ./staticfiles:/app/staticfiles
    env_file: .env
    ports:
      - "127.0.0.1:8001:8000"
    restart: unless-stopped